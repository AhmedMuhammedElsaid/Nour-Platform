// Relocated to @repo/shared-core (pure Intl-based formatting, shared with
// apps/mobile — requires Hermes Intl). Re-exported here so existing relative
// imports + the adjacent format.test.ts keep working unchanged.
export * from "@repo/shared-core/prayer-times/format";
