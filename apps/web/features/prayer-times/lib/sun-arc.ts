// Relocated to @repo/shared-core (pure geometry, shared with apps/mobile via
// react-native-svg). Re-exported here so existing relative imports + the
// adjacent sun-arc.test.ts keep working unchanged.
export * from "@repo/shared-core/prayer-times/sun-arc";
