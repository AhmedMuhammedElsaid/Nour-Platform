// Relocated to @repo/shared-core/prayer-times/compute (pure, isomorphic —
// shared with apps/mobile via Metro). Re-exported here so existing relative
// imports inside packages/api and the public `@repo/api/services/prayer-times`
// subpath keep working unchanged.
export * from "@repo/shared-core/prayer-times/compute";
