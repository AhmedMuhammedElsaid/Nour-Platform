// Relocated to @repo/shared-core (pure, shared with apps/mobile via Metro).
// Re-exported here so existing relative imports inside packages/api and the
// public `@repo/api/schemas/media` subpath keep working unchanged.
export * from "@repo/shared-core/schemas/media";
